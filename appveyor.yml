version: '1.0.{build}'

image: Visual Studio 2017

init:
  - vcpkg install sdl2:x64-windows
  - vcpkg integrate install

platform:
  - x64
 
configuration:
  - Debug

before_build:
  - cmake -DCMAKE_TOOLCHAIN_FILE=c:/tools/vcpkg/scripts/buildsystems/vcpkg.cmake -G "Visual Studio 15 2017 Win64" tests

build:
  project: $(APPVEYOR_BUILD_FOLDER)\tests.sln

test_script:
  - '%APPVEYOR_BUILD_FOLDER%\%CONFIGURATION%\01-1-hello-world.exe'

cache: c:\tools\vcpkg\installed\
